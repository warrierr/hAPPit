.dashboard-wrap
	.col-md-2
	.col-md-3
		.add-new-habit
			h3 Add Another Habit
			a(href="/create")
				.add-icon
		.archived-habit-button
			h3 Archived Habits
			h1 0
	.col-md-5
		h1#habits-header Habits
		ul.habit(ng-repeat="habit in habits")
			li.habit-item 
				h3 {{habit.name}}
				button.btn(ng-class="getButtonClass(habit.type)", ng-click="logHabit(habit)") {{getButtonText(habit.type)}}
	.col-md-2